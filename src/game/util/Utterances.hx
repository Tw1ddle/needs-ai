package game.util;

import needs.util.StringDataBuilder;

// Automatically builds strings and maps of attributes from files in the directory
@:build(needs.util.StringDataBuilder.build("../embed/utterances"))
@:keep
class Utterances
{
}